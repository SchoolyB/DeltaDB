module main

import & use @std, @arrays
import & use codec"../src/core/codec"
import & use lib"../src/library"
#suppress(ALL)


const DB_FILE string = "database.delta"

do main(){

    temp data [byte]
    temp err Error

    create_db_file(DB_FILE)

    data, err = lib.get_file_data(DB_FILE)
    if len(data) == 0 {
        println("No data in file")
    }


    //temp tStamp = get_timestamp()
    //temp result = serialize_u64(tStamp)
}

